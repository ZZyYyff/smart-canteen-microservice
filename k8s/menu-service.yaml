apiVersion: v1
kind: Service
metadata:
  name: menu-service
  namespace: smart-canteen
  labels:
    app: menu-service
spec:
  type: ClusterIP
  ports:
    - port: 9002
      targetPort: 9002
  selector:
    app: menu-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-service
  namespace: smart-canteen
  labels:
    app: menu-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: menu-service
  template:
    metadata:
      labels:
        app: menu-service
    spec:
      containers:
        - name: menu-service
          image: smart-canteen/menu-service:latest
          env:
            - name: MYSQL_HOST
              value: mysql
            - name: MYSQL_PORT
              value: "3306"
            - name: MYSQL_USER
              value: root
            - name: MYSQL_PASSWORD
              value: root
            - name: REDIS_HOST
              value: redis
            - name: NACOS_SERVER_ADDR
              value: nacos:8848
          ports:
            - containerPort: 9002
          readinessProbe:
            tcpSocket:
              port: 9002
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 9002
            initialDelaySeconds: 60
            periodSeconds: 20
