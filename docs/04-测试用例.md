# 测试用例

## 1. 通用模块测试 (common)

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-COM-01 | Result.success() 无数据 | 调用 Result.success() | code=200, message="操作成功", data=null |
| TC-COM-02 | Result.success() 带数据 | 调用 Result.success("data") | code=200, data="data" |
| TC-COM-03 | Result.fail() 默认码 | 调用 Result.fail("错误") | code=1001, message="错误" |
| TC-COM-04 | Result.fail() 自定义码 | 调用 Result.fail(3002, "库存不足") | code=3002 |
| TC-COM-05 | BusinessException 含 ErrorCode | new BusinessException(ErrorCode.NOT_FOUND) | code=404, message="资源不存在" |
| TC-COM-06 | BusinessException 自定义消息 | new BusinessException(ErrorCode.NOT_FOUND, "用户不存在") | code=404, message="用户不存在" |
| TC-COM-07 | JWT 生成并解析 | generateToken(1L, "STUDENT") → getUserId/getRole | userId=1, role="STUDENT" |
| TC-COM-08 | 有效 Token 验证 | isTokenValid(validToken) | true |
| TC-COM-09 | 无效 Token 验证 | isTokenValid("invalid") | false |
| TC-COM-10 | UserContextHolder 设置和获取 | set(UserContext) → get() | 值一致 |
| TC-COM-11 | ErrorCode 枚举值 | ErrorCode.values().length | 8 |
| TC-COM-12 | ErrorCode SUCCESS | ErrorCode.SUCCESS.getCode() | 200 |
| TC-COM-13 | OrderStatus 包含 6 种状态 | OrderStatus.values().length | 6 |
| TC-COM-14 | PickupQueueStatus 包含 4 种状态 | PickupQueueStatus.values().length | 4 |

## 2. 用户服务测试 (user-service)

### 2.1 Service 层单元测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-USER-01 | 注册成功 | phone=13900001111, password=123456, studentNo=2025001 | 返回 UserVO, role=STUDENT, status=NORMAL |
| TC-USER-02 | 重复手机号注册 | 手机号已存在 | 抛出 BusinessException |
| TC-USER-03 | 重复学工号注册 | 学工号已存在 | 抛出 BusinessException |
| TC-USER-04 | 手机号登录成功 | phone=13900001111, password=123456 | 返回 LoginVO，含 accessToken 和 refreshToken |
| TC-USER-05 | 学工号登录成功 | studentNo=2025001, password=123456 | 返回 LoginVO |
| TC-USER-06 | 密码错误 | 密码不匹配 | 抛出 BusinessException |
| TC-USER-07 | 用户不存在 | 不存在的手机号 | 抛出 BusinessException |
| TC-USER-08 | 账号禁用 | 用户 status=DISABLED | 抛出 BusinessException |
| TC-USER-09 | 查询当前用户 | 存在的 userId | 返回 UserVO |
| TC-USER-10 | 查询不存在的用户 | 不存在的 userId | 抛出 BusinessException |
| TC-USER-11 | 修改昵称 | userId=1, nickname="新昵称" | 返回更新后的 UserVO |
| TC-USER-12 | Token 刷新 | 有效的 refreshToken | 返回新的 dual tokens |
| TC-USER-13 | 无效 RefreshToken | 无效的 token | 抛出 BusinessException |
| TC-USER-14 | 无输入方式登录 | LoginDTO 无 phone 也无 studentNo | 抛出 BusinessException |

### 2.2 Controller 层集成测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-USER-15 | 注册成功 HTTP 200 | POST /api/users/register 含完整信息 | 200, data.phone=13900001111 |
| TC-USER-16 | 注册缺手机号 400 | POST /api/users/register 无 phone | 400 Bad Request |
| TC-USER-17 | 登录成功返回 Token | POST /api/users/login 含正确凭证 | 200, data.token 非空 |
| TC-USER-18 | 登录缺密码 400 | POST /api/users/login 无 password | 400 Bad Request |
| TC-USER-19 | 查询当前用户 | GET /api/users/me, X-User-Id=1 | 200, data.nickname 正确 |

## 3. 菜品与菜单服务测试 (menu-service)

### 3.1 Service 层单元测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-MENU-01 | 新增菜品 | name="红烧肉", price=25, stock=50 | 返回 DishVO, id 非空 |
| TC-MENU-02 | 上架菜品 | dishId=1 | status=ON_SALE |
| TC-MENU-03 | 下架菜品 | dishId=1 | status=OFF_SALE |
| TC-MENU-04 | 按状态筛选 | status=ON_SALE | 返回上架菜品列表 |
| TC-MENU-05 | 低库存预警 | stock <= warning_stock | lowStock=true |
| TC-MENU-06 | 库存充足扣减 | dishId=1, quantity=5 | 扣减成功，stock 减少 5 |
| TC-MENU-07 | 库存不足扣减 | dishId=1, quantity=999 | 抛出 BusinessException，stock 不变 |
| TC-MENU-08 | 恢复库存 | dishId=1, quantity=5 | stock 增加 5 |
| TC-MENU-09 | 今日菜单 | 有今日日期的 daily_menu | 返回菜单及菜品详情 |
| TC-MENU-10 | 今日无菜单 | 无今日日期的 daily_menu | 返回空列表 |
| TC-MENU-11 | 创建每日菜单 | 日期+餐次+菜品列表 | 返回 DailyMenuVO |
| TC-MENU-12 | 菜品不存在 | 查询不存在的 dishId | 抛出 BusinessException |

### 3.2 Controller 层集成测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-MENU-13 | 新增菜品 200 | POST /api/menus/dishes | 200, data.name 正确 |
| TC-MENU-14 | 查询菜品列表 | GET /api/menus/dishes | 200, data 长度=1 |
| TC-MENU-15 | 上架菜品 | PUT /api/menus/dishes/1/on-sale | 200, data.status=ON_SALE |
| TC-MENU-16 | 下架菜品 | PUT /api/menus/dishes/1/off-sale | 200, data.status=OFF_SALE |
| TC-MENU-17 | 扣减库存 | PUT /api/menus/dishes/1/stock/deduct, quantity=5 | 200 |
| TC-MENU-18 | 恢复库存 | PUT /api/menus/dishes/1/stock/restore, quantity=5 | 200 |

## 4. 订单服务测试 (order-service)

### 4.1 Service 层单元测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-ORD-01 | 下单成功 | 1 个菜品, quantity=2 | 返回 OrderVO, status=CREATED, 调用 Feign 扣库存 |
| TC-ORD-02 | 库存不足回滚 | 2 个菜品，第二个扣减失败 | 抛异常，回滚第一个的库存，订单未创建 |
| TC-ORD-03 | 多菜品总金额 | 25×2 + 10×3 = 80 | totalAmount=80.00 |
| TC-ORD-04 | 查询订单详情 | orderId=100 | 返回 OrderVO |
| TC-ORD-05 | 查询不存在的订单 | orderId=999 | 抛出 BusinessException |
| TC-ORD-06 | 取消订单 | orderId=100, status=CREATED | status=CANCELLED, 调用 Feign 恢复库存 |
| TC-ORD-07 | 已完成不可取消 | status=COMPLETED | 抛出 BusinessException |
| TC-ORD-08 | 接单 CREATED→ACCEPTED | status=CREATED | status=ACCEPTED |
| TC-ORD-09 | 备餐完成→调用 pickup | status=COOKING | status=WAIT_PICKUP, 调用 pickupFeignClient |
| TC-ORD-10 | 非法状态流转 | CREATED 直接跳到 COOKING | 抛出 ORDER_STATUS_ERROR |
| TC-ORD-11 | 完整状态流转 | CREATED→ACCEPTED→COOKING→WAIT_PICKUP→COMPLETED | 所有步骤通过 |
| TC-ORD-12 | 查询我的订单 | userId=1 | 返回订单列表 |

### 4.2 Controller 层集成测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-ORD-13 | 创建订单 200 | POST /api/orders, X-User-Id=1, 含 items | 200, data.status=CREATED |
| TC-ORD-14 | 创建订单缺 items → 400 | POST /api/orders, X-User-Id=1, 无 items | 400 |
| TC-ORD-15 | 查询订单详情 | GET /api/orders/100 | 200, data.id=100 |
| TC-ORD-16 | 查询我的订单 | GET /api/orders/my, X-User-Id=1 | 200, data 长度=1 |
| TC-ORD-17 | 取消订单 | PUT /api/orders/100/cancel | 200, data.status=CANCELLED |
| TC-ORD-18 | 接单 | PUT /api/orders/100/accept | 200, data.status=ACCEPTED |

## 5. 取餐服务测试 (pickup-service)

### 5.1 Service 层单元测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-PICK-01 | 新增窗口 | name="1号窗口", location="一楼A区" | 返回 PickupWindowVO |
| TC-PICK-02 | 查询窗口列表 | — | 返回列表 |
| TC-PICK-03 | 启用窗口 | windowId=1 | status=ACTIVE |
| TC-PICK-04 | 停用窗口 | windowId=1 | status=DISABLED |
| TC-PICK-05 | 操作不存在的窗口 | windowId=999 | 抛出 BusinessException |
| TC-PICK-06 | 加入队列 | orderId=100, windowId=1, pickupNo=123 | queue 记录创建 |
| TC-PICK-07 | 查询队列（按 queue_time 升序） | windowId=1 | 按时间排序的队列 |
| TC-PICK-08 | 空队列 | windowId=1 | 返回空列表 |
| TC-PICK-09 | 叫号成功 | windowId=1 有 WAITING 记录 | 最早的记录 → CALLED |
| TC-PICK-10 | 空队列叫号 | windowId=1 无 WAITING 记录 | 抛出 BusinessException |
| TC-PICK-11 | 错误取餐码核销 | pickupCode 不匹配 | 抛出 BusinessException(5001) |
| TC-PICK-12 | 正确核销 | pickupNo+pickupCode 正确 | → FINISHED, 调用 order-service |
| TC-PICK-13 | 取餐记录不存在 | 不存在的 pickupNo | 抛出 BusinessException |
| TC-PICK-14 | 已完成不可重复核销 | status=FINISHED | 抛出 BusinessException(5002) |

### 5.2 Controller 层集成测试

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-PICK-15 | 新增窗口 200 | POST /api/pickup/windows | 200, data.name 正确 |
| TC-PICK-16 | 查询窗口列表 | GET /api/pickup/windows | 200, data 长度=1 |
| TC-PICK-17 | 查询队列 | GET /api/pickup/windows/1/queue | 200, data[0].pickupNo=123 |
| TC-PICK-18 | 叫号 | POST /api/pickup/windows/1/call-next | 200, data.status=CALLED |
| TC-PICK-19 | 核销成功 | POST /api/pickup/verify, 正确码 | 200, data.status=FINISHED |
| TC-PICK-20 | 核销缺字段 → 400 | POST /api/pickup/verify, 无 pickupNo | 400 |

## 6. 网关服务测试 (gateway-service)

| 编号 | 测试目标 | 输入 | 预期结果 |
|------|---------|------|---------|
| TC-GW-01 | 登录路径放行 | GET /api/users/login 无 Token | chain.filter 被调用 |
| TC-GW-02 | 注册路径放行 | POST /api/users/register 无 Token | chain.filter 被调用 |
| TC-GW-03 | 刷新 Token 放行 | POST /api/users/refresh-token 无 Token | chain.filter 被调用 |
| TC-GW-04 | 无 Token → 401 | GET /api/orders/123 无 Authorization | response.status=401 |
| TC-GW-05 | 无 Bearer 前缀 → 401 | Authorization="invalid-token" | response.status=401 |
| TC-GW-06 | 无效 Token → 401 | Authorization="Bearer invalid" | response.status=401 |
| TC-GW-07 | 有效 Token 通过 | Authorization="Bearer validToken" | chain.filter 被调用 |
| TC-GW-08 | 正确注入请求头 | Token(userId=2, role=MERCHANT) | X-User-Id=2, X-User-Role=MERCHANT |
